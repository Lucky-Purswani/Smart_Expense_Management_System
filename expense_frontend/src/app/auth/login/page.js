import LoginForm from "./LoginForm";

export const metadata = {
  title: "Login | Expense Manager",
};

// Server component — renders the client-side login form
export default function LoginPage() {
  return <LoginForm />;
}
